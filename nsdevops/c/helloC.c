#include <stdlib.h>
#include <string.h>
#include <stdio.h>

int main(){
        printf("hello");
		system("gtacl -c 'sutver'");
        system("gtacl -c 'sysinfo'");
        system("/bin/uname -a");
        return 0;
}